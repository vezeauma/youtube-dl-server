version: "3.9"
services: 
  youtube-dl:
    build:
      context: .
    user: "1000:1000"
    ports: 
      - 8080:8080
    volumes:
      - /data/sambashare:/youtube-dl
      - /tmp/youtube-dl:/tmp/youtube-dl
    restart: always
